# ===== HR Harness 后端核心依赖（可直接运行的最小集） =====
# 运行: pip install -r requirements.txt

# Web 框架
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# 配置与校验
pydantic>=2.7
pydantic-settings>=2.3

# 安全（JWT）
PyJWT>=2.8

# 数据库（开发默认 SQLite，生产切 MySQL）
SQLAlchemy>=2.0
pymysql>=1.1

# HTTP 客户端（公网 LLM / 推理适配层）
httpx>=0.27

# 制度、合同与简历文档解析
pypdf>=5.0
python-docx>=1.1

# 测试
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0

# 重型本地模型与 Milvus 依赖见 requirements-optional.txt，默认不安装。
